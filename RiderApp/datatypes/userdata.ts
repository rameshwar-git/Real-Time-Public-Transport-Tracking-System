export type UserData = {
    name: string;
    dob: string;
    gender: string;
    email: string;
    phone: string;
    password: string;
};

export type LoginData = {
    email: string;
    password: string;
}